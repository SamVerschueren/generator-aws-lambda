{
	"test": {<% if (includeDynongo) { %>
		"DynamoDB": {
			"prefix": "test",
			"host": "dynamodb",
			"local": true
		}<% } %>
	},
	"staging": {<% if (includeDynongo) { %>
		"DynamoDB": {
			"prefix": "staging"
		}<% } %>
	},
	"production": {<% if (includeDynongo) { %>
		"DynamoDB": {
			"prefix": "production"
		}<% } %>
	}
}
