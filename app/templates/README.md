# <%= functionName %>

> <%= functionDescription %>

## Usage

Create a zip file that can be deployed

```
$ npm run zip
```

Deploy the zip file generated in the `dist` directory.

## Author

- <%= name %> [<<%= email %>>]
